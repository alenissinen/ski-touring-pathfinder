package application;

/** Defines how mouse input is handled */
public enum MouseMode {
    /** Mouse controls camera orientation, hidden cursor */
    FLIGHT,
    /** Mouse is free for point selection, visible cursor */
    SELECTION
}
