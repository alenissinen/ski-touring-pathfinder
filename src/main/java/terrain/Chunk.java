package terrain;

/**
 * Represents a 32x32 section of the merged {@link HeightMap}.
 * Manages its own OpenGL resources (VBO, VAO) and handles data sharing
 * to and from the GPU.
 */
public class Chunk {
}
